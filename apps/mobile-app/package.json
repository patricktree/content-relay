{
  "name": "@content-relay/mobile-app",
  "private": true,
  "scripts": {
    "build": "turbo run turbo:build",
    "turbo:build": "pnpm run native:sync",
    "native:sync": "cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@capacitor/ios": "^8.3.1",
    "@content-relay/web-app": "workspace:*"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.1",
    "@content-relay/config-typescript": "workspace:*",
    "@types/node": "catalog:"
  }
}
