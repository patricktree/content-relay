{
  "name": "@content-relay/macos-app",
  "private": true,
  "scripts": {
    "build": "cp ./node_modules/@content-relay/client/dist/content-relay-hub-openapi.json ./Sources/RelayOpenAPI/openapi.json && ./scripts/build.sh",
    "clean": "del-cli --dot=true \".build\" \"dist\"",
    "lint": "swift build -c debug -Xswiftc -warnings-as-errors",
    "lint:fix": "pnpm run lint",
    "test": "swift test"
  },
  "devDependencies": {
    "@content-relay/client": "workspace:*"
  }
}
